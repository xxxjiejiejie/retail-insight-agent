# Retail Insight Agent package stub

This stable file satisfies the package metadata readme requirement while Docker installs
dependencies. The real project README and application source are copied in later layers.
